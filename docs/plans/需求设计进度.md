# VisionHyperAgent 需求设计进度

## 1. 当前工作

| 项目 | 内容 |
| --- | --- |
| 本次任务类型 | 按新需求技能做首次规范化整理，同时核对该仓库已有需求与实现进展 |
| 用户本次要求 | 阅读当前项目的需求和进展，用 project-requirement 技能重新梳理需求 |
| 当前阶段 | 技术方案设计 |
| 当前章节或模块 | 5. 场景与数据流 |
| 下一步 | 用户确认第 5 章后，进入第 6 章《异常处理与安全边界》 |
| 阻塞或待用户确认事项 | 第 5 章启动恢复、项目导入、标注、数据集、训练、评估、推理和退出清理流程待整体确认 |

## 2. 阶段状态

| 阶段 | 状态 | 产物 | 确认或检查依据 |
| --- | --- | --- | --- |
| 准备 | 已完成 | brainstorming 可用，当前进度记录 | 已读取 /home/xyf/.codex/skills/brainstorming/SKILL.md，未重复下载 |
| 产品需求 | 已完成 | docs/产品需求文档.md | 用户已确认本文档作为首版产品需求基线；性能验收、模型服务依赖、数据集范围、辅助预标注模型范围和无 GPU 体验范围均已确认 |
| 技术方案设计 | 进行中 | docs/技术方案设计.md | 第 1 至 4 章已确认，第 5 章草案已生成；旧架构资料仅作现状参考，不直接继承为最终方案 |
| 模块 API | 未核对 | docs/models/<架构层名称>/<模块名>.md 未创建；旧资料为 docs/frontend-backend-architecture.md | 旧资料仅作现状参考，不替代新流程产物 |
| 项目结构 | 未核对 | docs/项目结构.md 未创建 | 仓库目录存在，但尚未按新模板汇总确认 |
| 代码规范 | 未核对 | docs/代码规范.md 未创建 | 现有源码有事实规范，尚未按技能生成正式规范文档 |
| 文档路由 | 待更新 | 根目录 AGENTS.md | 当前路由到 project-requirement 技能；待正式产物完成后再合并更新，不在需求草案阶段提前覆盖 |

## 3. 技术方案逐章确认

| 章节完整标题 | 状态 | 已确认结论摘要 | 用户确认依据 | 尚待确认或发生变化的内容 |
| --- | --- | --- | --- | --- |
| 1. 概述 | 已确认 | Rust 本地宿主 + Svelte Web 前端 + Python ML Worker；项目目录保存大文件，SQLite 保存元数据；Windows 输出 bin/win/ zip，Linux 输出 bin/linux/ 绿色目录 | 用户明确回复 “确认” | 无 |
| 2. 整体架构 | 已确认 | 浏览器、Rust 宿主、Python Worker、Codex、模型服务和本地存储分层明确；Worker 不直接访问 SQLite，Rust 统一拥有业务数据；任务状态机和本地数据布局已确认 | 用户明确回复 “确认” | 无 |
| 3. 模块职责划分 | 已确认 | 已划分 workspace-ui、annotation-canvas、api-gateway、project-service、ml-service、agent-service、task-runtime、storage、desktop-host、common-services、mask-rcnn-worker 共 11 个核心模块，并明确职责边界和依赖规则 | 用户明确回复 “确认” | 无 |
| 4. 三方库集成策略 | 已确认 | 前端、Rust、Python、桌面、测试和打包依赖的集成位置与隔离边界已确认；GPU 绿色包内置 CUDA 运行库但不禁用、不安装 NVIDIA 驱动 | 用户明确回复 “可以” | 锁定 PyTorch wheel 后补充最低 NVIDIA 驱动版本 |
| 5. 场景与数据流 | 草案待确认 | 已规划启动恢复、项目导入、Agent 规则、人工标注、数据集、GPU 检测、训练、评估、推理、取消恢复和退出清理流程 | 无 | 各场景步骤、数据产物、事件流和一致性规则待整体确认 |

## 4. 模块与产物进度

| 架构层 / 模块 | 文档路径 | 状态 | 未完成的检查或同步 |
| --- | --- | --- | --- |
| 无 | 无 | 未开始 | 待架构设计确认后建立模块清单，再生成模块 API 文档 |

### 使用前检查口径

| 记录情况 | 对本次任务的处理 |
| --- | --- |
| 相关 API 已完成、上游所需确认有效、关联变更已同步，且记录与正文一致 | 可以据此实施，不重复采访已确认事项 |
| 相关契约仍在生成、待确认、待更新或阻塞，或关联变更尚未同步 | 暂不依据该契约实施，转需求技能处理对应的未完成事项 |
| 进度文件或模块记录缺失、状态未核对、记录与正文不一致 | 核实本次相关基线后补充记录，不默认为可用，也不从头重做无关需求访谈 |
| 待更新事项与本次模块及依赖契约无关 | 不阻塞已经核对有效的模块，不重置其状态 |

## 5. 当前变更影响与同步

| 变更点 | 最早受影响阶段 | 确认结论或待确认决定 | 待同步文档 | 当前处理结果 |
| --- | --- | --- | --- | --- |
| 将旧 PRD 中的技术选型、通信协议、目录实现从产品文档剥离 | 产品需求 | 技术内容不再写入新 PRD；技术方案信息暂留旧架构资料，待技术方案阶段核对 | docs/产品需求文档.md、后续 docs/技术方案设计.md | 产品草案已剥离；旧技术方案未改写 |
| 交付形态从“单可执行文件”修订为“绿色压缩包” | 产品需求 | 用户已明确以后提供压缩包，不单独提供裸 exe；首版承诺 Windows 和 Linux，macOS 不纳入验收 | docs/产品需求文档.md | 已按 Windows / Linux 压缩包和平台范围回写 |
| Codex 端口改为运行期随机分配，不作为用户配置项 | 技术方案设计 | 用户已确认删除固定端口并自动重试随机端口 | 旧 docs/architecture.md、使用说明已描述现状；后续新技术方案稿待同步 | 已记录为实现事实，未提前生成技术方案稿 |
| Agent 前后端、附件、审批、历史和生命周期已实机验证 | 产品需求 / 技术方案设计 | 测试报告记录 Linux 与真实模型验证；Windows 仅交叉构建和逻辑验证，未实机验证托盘生命周期 | docs/产品需求文档.md、后续技术方案与 API 文档 | PRD 验收按用户可感知结果记录，不宣称 Windows 实机通过 |
| 产品定位修订为“视觉数据标注与训练工具，Agent 主导并辅助人的工作” | 产品需求 | 用户明确确认该定位；Agent 主导理解、澄清、生成和建议，用户确认关键结果与决策 | docs/产品需求文档.md | 已回写定位、目标和人机职责 |
| 目标用户修订为“所有人” | 产品需求 | 用户明确确认产品面向所有有视觉识别需求的用户，不按行业或职业限制入口；不同人群仅作为典型场景分层 | docs/产品需求文档.md | 已按通用入口加典型场景回写 |
| 首版核心场景确认 | 产品需求 | 首版先按“导入图片 → 描述目标 → 规则确认 → 辅助初标 → 修正确认 → 数据集 → 训练 → 评估 → 测试”确认；后续用户进一步明确首版不接入 Segment Anything 等辅助预标注模型 | docs/产品需求文档.md | 已按最终口径修订为人工实例标注、内部数据集、训练、评估和推理测试主线 |
| 首版模型任务确认 | 产品需求 | 用户明确确认首版任务为实例分割，理由是实际使用场景较多；每个目标需要类别、独立实例和像素级遮罩 | docs/产品需求文档.md | 已回写产品范围 |
| 小样本策略确认 | 产品需求 | 用户明确要求不阻止用户体验流程；数据量不足时提供风险提示和建议，不作为发起实验训练的产品门槛 | docs/产品需求文档.md | 已写入异常反馈规则 |
| Agent 主导边界确认 | 产品需求 | 用户选择高自主模式，并进一步明确每次执行前 Agent 都必须提交执行计划，包括参数；用户确认后才执行，偏离计划需重新确认 | docs/产品需求文档.md | 已回写定位、职责、计划确认和执行审计要求 |
| 执行计划粒度确认 | 产品需求 | 用户确认采用阶段级打包计划：一个阶段一次确认，计划内列明步骤和参数；确认后连续执行，偏离计划再确认 | docs/产品需求文档.md | 已回写 Agent 对话与协作需求 |
| 执行计划交互形态确认 | 产品需求 | 用户明确不使用计划卡片，执行计划和参数调整都通过纯自然语言交互完成 | docs/产品需求文档.md | 已改为自然语言计划说明、确认和调整 |
| 首版完成度确认 | 产品需求 | 用户确认首版做完整闭环，每个环节保持最小可用；不把离线部署导出、账号协作和额外任务类型纳入首版验收主线 | docs/产品需求文档.md | 已回写交付口径、模型测试范围和排除项 |
| 性能验收设备确认 | 产品需求 | 用户确认采用 Windows 10/11、Linux 主流发行版、32GB 内存、16GB 显存作为首版性能验收基准 | docs/产品需求文档.md | 已回写体验约束与待确认表 |
| 模型服务交付方式确认 | 产品需求 | 用户确认首版不内置模型服务，由用户自行配置模型服务 | docs/产品需求文档.md | 已回写模型服务配置和待确认表 |
| 首版辅助分割模型范围确认 | 产品需求 | 用户确认产品本身负责训练与推理；首版不考虑 Segment Anything 等辅助预标注模型，后续再评估。首版不自动生成候选遮罩，Agent 负责规则、建议、训练参数和问题分析 | docs/产品需求文档.md | 已同步定位、主线、标注辅助、数据集范围、业务规则和验收场景 |
| 无 GPU 体验范围确认 | 产品需求 | 用户确认无 GPU 时项目、图片、标注和 Agent 对话仍可用；训练与推理不承诺可用，完整训练与推理按 32GB 内存、16GB 显存设备验收 | docs/产品需求文档.md | 已回写训练资源处理、异常反馈和待确认表 |
| 技术方案模板新增开发计划章节 | 技术方案设计 | 用户要求技术方案设计增加开发计划章节，描述模块开发顺序、每个模块的功能需求、测试要求、实际交付物、里程碑与集成验收 | project-rules/project-requirement/templates/技术方案模板.md、后续 docs/技术方案设计.md | 模板已增加第 7 章；待技术方案阶段按 VisionHyperAgent 实际模块逐项确认 |
| 总体技术路线确认 | 技术方案设计 | 用户确认方案 A：Svelte Web 前端 + Rust 本地宿主 + Python ML Worker；Rust 负责宿主、服务和生命周期，Python 负责 PyTorch / torchvision Mask R-CNN 训练、评估和推理 | docs/技术方案设计.md | 第 1 章草案已生成，待用户确认后进入整体架构设计 |
| 本地数据存储方案确认 | 技术方案设计 | 用户确认采用“项目目录保存大文件 + SQLite 保存本地元数据和状态”的方案；SQLite 为嵌入式本地文件，不启动数据库服务，不需要用户安装 | docs/技术方案设计.md | 已回写第 1 章技术栈说明 |
| 构建产物目录调整 | 产品需求 / 技术方案设计 | VisionHyperAgent 用户确认 bin 分为 win 与 linux 两个目录：Windows 在 bin/win/ 输出绿色版 zip；Linux 在 bin/linux/ 直接输出绿色软件目录，不再二次压缩 | docs/产品需求文档.md、docs/技术方案设计.md、project-rules/project-build.md | 项目需求和技术方案已记录具体目录；project-build 已改为通用规则，构建时以项目确认文档为准；后续实施阶段需同步修改实际构建脚本 |
| GPU 绿色包运行策略确认 | 技术方案设计 | 用户确认绿色包内置 Python、PyTorch CUDA、torchvision 和 CUDA 运行库；主机已安装满足要求的 NVIDIA 官方驱动时自动使用 GPU，不要求用户安装 Python 或 CUDA Toolkit。应用不自动安装显卡驱动，缺少驱动时明确提示 | docs/技术方案设计.md | 已回写第 4 章 PyTorch 集成、绿色包约束和依赖风险；最低驱动版本待锁定 PyTorch wheel 后补充 |
| 产品需求文档重整 | 产品需求 | 汇总已确认定位、目标用户、实例分割任务、首版主线、阶段级自然语言计划、性能基准、模型服务依赖、数据集范围、辅助预标注范围、无 GPU 体验和最小闭环口径 | docs/产品需求文档.md | 用户已确认作为首版产品需求基线 |

## 6. 已有资料与实现进展核对

| 主题 | 现状 | 对新需求稿的影响 |
| --- | --- | --- |
| 旧产品需求 | docs/product-requirements.md 标记已确认，但混合产品、部署、协议、技术选型和里程碑 | 作为需求来源保留；新稿只保留用户目标和产品行为 |
| 架构基线 | docs/architecture.md、docs/frontend-backend-architecture.md 描述 Web 前端、本地后端、日志、配置、接口和模块 | 技术方案暂不进入 PRD，等待架构阶段逐章核对 |
| 界面评估 | docs/agent-interface-assessment.md 的“未接入真实链路”结论已被后续实现超越 | 以测试报告和当前源码为准，避免把已实现能力继续列为缺口 |
| Agent 接入 | 聊天、流式、停止、模型 / Effort、历史、附件、审批、问题回答、配置和状态恢复已接入并验证 | 列入当前能力；Windows 实机行为仍待验收 |
| 进程生命周期 | 后端拥有 Agent 运行环境，托盘退出回收；网页关闭不退出服务；随机端口避免冲突 | 作为产品生命周期规则与验收场景 |
| 日志与配置 | 全局日志、本地 JSON 配置、多 Provider / 多模型、密钥不回显已有实现说明 | 转换为可理解、安全、可修复的产品约束，不写协议细节 |
| 视觉业务 | 预标注、预训练、标注、训练、推理页面存在，但业务闭环仍处于后续阶段 | 列为后续必需能力，不误报为已完成 |
| 测试与交付 | Linux 包、Windows 包、自动化测试和真实模型链路已有报告；Windows 未实机验证 | 只把已验证内容写入进展，不夸大平台覆盖 |
