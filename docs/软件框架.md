# 软件框架

| 项目 | 内容 |
| --- | --- |
| 版本 | 0.6 |
| 日期 | 2026-09-07 |
| 状态 | 架构与目录基线已确认，工程骨架已建立；各层业务模块后续补充 |
| 范围 | 整体形态、六层职责、调用关系、Python 归属、状态边界及目录规划 |

本文确定软件框架与目录分层，不列各层具体业务模块或接口。当前已获准初始化单 Cargo 包、分层入口和配套目录；只验证最小启动行为，不实现业务功能、不安装运行依赖或生成实施计划。

## 1. 总体选择：先保持简单

- 采用模块化单体，以一个 Rust + Slint 主程序承载软件交互和业务控制。
- 首版不拆分前端、后端两个主可执行程序，不建立前后端 ZeroMQ 通信或内部 ZeroMQ 总线。
- Codex CLI 和 Python Worker 可以作为软件管理的内部执行进程，不作为需要用户分别部署的后端产品。
- ZeroMQ 保留为对外图片识别接口；相机输入继续采用 OpenCV。
- 保持方案 A：私有 Python/PyTorch 环境训练 YOLO26，Rust 执行生产推理。

六层表示职责，不要求每次调用逐层经过，也不要求每层独立成为进程、crate 或顶层目录。

## 2. 六层职责

| 层级 | 核心职责 | 边界 |
| --- | --- | --- |
| 1. 用户交互层（View） | 使用 Slint 呈现内容，接收按钮、对话和画布等用户输入。 | 不直接操作训练、推理、相机或通信实现。 |
| 2. 展示逻辑层（ViewModel） | 管理界面状态与数据绑定，将用户输入转换为请求，呈现进度与结果。 | 不承担训练、标注、模型管理等业务规则；可调用应用服务或启动 Agent 工作流。 |
| 3. Agent 层 | 管理 Codex 交互、Skills、任务规划和工具调用，执行智能工作流编排。 | 使用应用能力，不另写一套业务实现，不拥有 Python 环境管理职责。 |
| 4. 应用层 | 提供统一业务能力，维护业务规则、任务生命周期、确认门槛和真实状态。 | 不直接绑定外部库的调用细节，不把业务状态交给界面或 Agent 文本自行决定。 |
| 5. 驱动／适配层 | 封装外部技术和执行程序的具体操作，将业务请求转换为实际执行与结果。 | Python Worker、YOLO 调用、Rust 推理、OpenCV、ZeroMQ 的调用适配归于此层；不直接操作界面。 |
| 6. 基础层 | 提供日志、配置、路径、通用资源及进程支撑，管理私有 runtime 的定位与校验。 | 不承载模型排名、训练策略或标注规则等业务决策。 |

表中的技术名称用于说明归属，不是已经确定的业务模块清单。各层具体模块、类、接口和模块内文件将在后续逐层设计。

## 3. Python 相关职责归属

| 内容 | 归属 |
| --- | --- |
| Python 解释器、依赖环境的定位、版本与完整性管理 | 基础层 |
| Python Worker、YOLO 训练/评估/导出调用及执行结果处理 | 驱动／适配层 |
| 训练任务的业务接口、状态和执行约束 | 应用层 |
| 训练策略、调参决策及工具调用 | Agent 层 |

这不是把 Python 物理拆成多套环境。软件仍管理一套与发行版匹配的私有训练环境，使用者不需要自行配置 Conda 或系统 Python。

Agent 使用 Python 支撑的业务能力，不因此拥有解释器、依赖安装或运行环境生命周期的管理职责。

## 4. 按钮与对话共享业务能力

软件的业务功能需要同时支持界面操作和对话操作。应用层提供唯一业务实现，两个入口复用相同的校验、执行和状态变化。

### 4.1 普通操作

- 按钮入口：View → ViewModel → 应用服务 → 驱动／适配。
- 对话入口：View → ViewModel → Agent → 工具接口 → 同一个应用服务 → 驱动／适配。

普通操作不必先经过大模型。例如，按钮和对话都可以触发同一相机控制能力，但按钮无需为了完成该操作先调用 Agent。

### 4.2 智能操作

- 自动标注、自动调参等智能按钮启动相应 Agent 工作流。
- 对话入口由 Agent 理解任务后进入同一工作流。
- Agent 通过工具接口使用应用服务，不为按钮再实现一套独立的智能编排。

### 4.3 Skills 与工具的关系

- Skills 指导软件功能的使用方式、任务规则和工作流程。
- 工具接口把 Agent 的调用映射到应用能力，不复制业务实现。
- 软件功能需要有对应的 Agent 调用能力和技能指引，但不要求每个按钮都建立一个独立 Skill。
- 强制业务规则由应用层校验；例如，Agent 工具不能绕过训练前的用户标注确认。

具体能力清单、工具名称、输入输出和 Skill 组织方式，留待各层模块设计时确定。工具接入协议尚未选定，不在本框架中锁定具体实现。

## 5. 关键业务流程

### 标注与人工确认

用户导入图片并说明要求 → Agent 生成初标 → 应用层登记标注 → 界面呈现 → 用户修改并确认 → 应用层记录可供训练使用的状态。

不额外引入独立辅助标注模型。初标质量和可编辑区域的表达方式仍按 PRD 的未决事项后续验证，不因确认架构就宣称已经解决。

### 自动训练与模型产出

用户确认并发起训练 → Agent 制定候选参数 → 应用层创建和管理任务 → 驱动层调用 Python Worker → 执行器返回指标与产物 → 应用层登记结果 → Agent 决定继续调参或选择模型 → 完成部署产物导出与验证。

Agent 负责策略，应用层负责真实状态与约束，执行器负责实际计算。不能仅凭 Agent 的“训练完成”回复判定成功。指标、预算和停止条件仍需后续确认，不在框架中指定默认值。

### 离线识别

- 相机：OpenCV 采集 → 输入适配 → 应用层推理服务 → Rust 推理适配 → 识别结果。
- 外部：ZeroMQ 收图 → 输入适配 → 同一个推理服务 → Rust 推理适配 → ZeroMQ 返回结果。

识别路径不经过 Agent，也不依赖启动 Python 训练进程。此处的 ZeroMQ 是对外识别接入，不是前后端内部通信。

## 6. 依赖与状态规则

- 核心业务通过明确接口使用执行能力，具体技术由适配实现承担，不让业务代码散落第三方调用细节。
- 外部输入或后台结果通过接口、回调或状态通知进入业务流程，驱动层不直接修改 View 或 ViewModel。
- 应用层维护统一业务状态；ViewModel 展示状态的投影，Agent 通过工具获取或改变同一份业务状态。
- Agent 操作引发的变化需要反映到界面，不能存在“对话状态”和“按钮状态”两套互不一致的事实。
- 基础能力可被需要的层使用，不是所有调用必须经过的最终业务步骤。
- 阻塞计算与等待不放在界面事件线程。具体调度方式、训练与推理是否并行及资源策略，后续再定。

## 7. 本地运行与联网边界

主程序、Codex CLI、Skills 和所需 runtime 由同一软件交付与管理。内部辅助进程不改变单主程序的产品形态，也不要求用户另行搭建服务。

沿用已经确认的联网边界：Agent 交互和初标允许调用配置的在线大模型服务并发送任务图片；训练计算在本机；生产识别可在不依赖互联网和在线 Agent 的条件下运行。

“Codex 本地内置”不被写成“当前已经支持完全离线的大模型交互”。本框架不增加本地大模型后端或额外视觉模型。

## 8. 目录分层

### 8.1 单 Cargo 包与目标目录树

Rust 侧先采用一个 Cargo 包，不拆成多成员 workspace。六层通过目录和模块边界组织，Python 训练源码作为独立的语言工程保留在 `depoly/packaging/python/` 下。

目录位置不改变架构职责：Python 训练执行仍属于驱动／适配层，私有解释器与依赖环境的管理仍属于基础层。

以下骨架目录已经建立，尚未创建的工程文件在树中明确标注。Rust 主程序保留启动提示并已接入异步日志及自动退出收尾；基础层已实现日志与配置单例，专项测试按用户要求删除，保留验证记录。空的源码、资源与打包目录用 `.gitkeep` 保留，业务功能尚未实现。

```text
VisionHyperAgent/
├── AGENTS.md                    # 开发 Agent 指引与目录索引
├── README.md
├── LICENSE
├── .gitignore
├── .cargo/
│   └── config.toml              # Cargo 输出目录：depoly/target
├── Cargo.toml                   # 单个 Rust 包及显式测试目标
├── Cargo.lock
├── build.rs                     # 后续接入 Slint 时创建，目前不存在
│
├── src/
│   ├── main.rs                  # 唯一主程序的启动与组件组装
│   ├── lib.rs                   # 内部模块入口与可测试代码组织
│   ├── view/                    # 第 1 层：View，实际组件见 8.1.1
│   │   ├── MainWindow.slint     # 主窗口入口
│   │   ├── Theme.slint          # 主题
│   │   ├── UiHints.slint        # 悬停提示状态
│   │   ├── ViewTypes.slint      # 视图枚举和数据结构
│   │   ├── components/          # 一组件一文件，文件名与组件名一致
│   │   ├── pages/               # 一页面一文件
│   │   └── resources/           # 原资源目录整体迁入，保留内部内容
│   │       ├── ui/              # 图标、字体等界面资源占位
│   │       └── agent/           # 产品 Agent 的指令与技能资源占位
│   ├── view_model/              # 第 2 层：展示逻辑
│   ├── agent/                   # 第 3 层：Agent
│   ├── application/             # 第 4 层：应用
│   ├── drivers/                 # 第 5 层：驱动／适配
│   ├── foundation/              # 第 6 层：基础，仅保留以下三个文件
│   │   ├── mod.rs              # 初始化、导出、错误上下文及路径定位
│   │   ├── logger.rs           # LOGGER 单例
│   │   └── config_manager.rs   # CONFIG 单例与配置数据
│   └── depends/                 # 第三方依赖材料，不是业务模块
│       └── README.md            # 管理约定；当前没有下载依赖
│
├── depoly/                      # 打包、辅助工具、测试与编译缓存
│   ├── packaging/
│   │   ├── python/              # Python 训练源码与包配置
│   │   │   ├── pyproject.toml   # 后续创建，目前不存在
│   │   │   ├── src/
│   │   │   │   └── vision_trainer/
│   │   │   └── tests/          # Python 包测试
│   │   ├── windows/             # Windows 打包配置
│   │   ├── linux/               # Linux 打包配置
│   │   └── runtime/             # 环境组装规则、依赖与兼容清单
│   ├── tools/                   # 开发、检查与构建辅助工具
│   ├── tests/                   # Rust 集成测试，Cargo.toml 显式注册
│   │   ├── smoke.rs             # 最小启动行为测试
│   │   └── fixtures/            # 小型、可公开的合成测试素材
│   └── target/                  # Cargo 生成内容，不提交 Git
│
├── bin/                         # 归集后的编译产物，不提交 Git
│   ├── windows/                 # 预留目录，尚未生成 Windows 产物
│   └── linux/                   # 本机骨架可执行文件及后续配套运行文件
│
└── docs/
    ├── 产品需求文档.md
    ├── 软件框架.md
    └── 团队编码规范.md
```

`main.rs` 和 `lib.rs` 是同一 Rust 包内的入口安排，不表示前后端两个主程序。`src/view/` 存放界面源码，`src/view/resources/` 是统一资源位置，原有 `ui/` 和 `agent/` 两个资源占位均保留。

资源目录并非全部属于界面：`src/view/resources/ui/` 面向界面资源，`src/view/resources/agent/` 面向产品内部 Codex。物理位置不将 Agent 资源的业务职责归入 View，也不意味着已经实现相应资源加载功能。

`depoly/` 按项目约定集中放置打包、工具、测试和缓存，不是新增架构层或独立后端。`.cargo/config.toml` 已将 Cargo 输出指向 `depoly/target/`；Rust 集成测试位于 `depoly/tests/`，必须在 `Cargo.toml` 中显式注册测试目标。`src/depends/` 只存第三方依赖材料，不在内部模块入口中声明为业务模块。

### 8.1.1 View 组件与实际文件

这是实际文件组织，不是运行时父子组件树。所有 UI 组件使用 `组件名.slint`，一组件一文件；页面专用的小组件也独立存放在 `components/`。视图枚举和数据结构不是 UI 组件，集中保存在 `ViewTypes.slint`。

```text
src/view/
├── MainWindow.slint
├── Theme.slint
├── UiHints.slint
├── ViewTypes.slint
├── components/
│   ├── AppHeader.slint
│   ├── DraftField.slint
│   ├── EmptyChart.slint
│   ├── EmptyState.slint
│   ├── ExpandableNavItem.slint
│   ├── FeatureTextArea.slint
│   ├── GlassPanel.slint
│   ├── GlobalChat.slint
│   ├── GridCanvas.slint
│   ├── IconButton.slint
│   ├── InfoRow.slint
│   ├── ModelCells.slint
│   ├── ModelRow.slint
│   ├── NavItem.slint
│   ├── StatusPill.slint
│   ├── ToolbarDivider.slint
│   └── WorkspaceHeader.slint
├── pages/
│   ├── AboutPage.slint
│   ├── AnnotationPage.slint
│   ├── InferencePage.slint
│   ├── ModelsPage.slint
│   ├── PreannotationPage.slint
│   ├── PretrainingPage.slint
│   ├── SettingsPage.slint
│   └── TrainingPage.slint
└── resources/
    ├── ui/
    └── agent/
```

- `MainWindow` 继续装配导航、页面、全局聊天和临时提示；导航区和提示的装配代码仍在主窗口内，不把逻辑区域名称当成不存在的类名。
- `ViewTypes.slint` 包含 `ViewPage`、`ModelEntry`、`FeatureDraft`、`TrainingDraft`；`Theme.slint`、`UiHints.slint` 分别导出同名全局对象。
- `ModelCells`、`ModelRow` 和 `FeatureTextArea` 从页面内移出后，作为组件供对应页面导入，不改变数据绑定或交互。
- Rust `view_model` 仍是骨架。本次只整理 View 文件结构，不转移状态所有权、不增加业务接入。

日志与配置的实现约定见 [Logger](plans/foundation/Logger.md) 和 [ConfigManager](plans/foundation/ConfigManager.md)。主程序已通过 `foundation::with_logging` 托管日志初始化及正常退出收尾；配置初始化和 Slint 界面仍未接入。内部线程、队列和生命周期守卫保留在 `logger.rs`，不另建模块；未经明确授权，不在 `src/foundation/` 新增其他文件。

### 8.2 Python、资源与基础层的对应

| 内容 | 目录位置 | 职责归属 |
| --- | --- | --- |
| Python 训练源码 | `depoly/packaging/python/src/vision_trainer/` | 驱动／适配层中的训练执行部分 |
| Rust 调用 Python 的适配代码 | `src/drivers/` | 驱动／适配层 |
| 私有 runtime 定位、校验与管理代码 | `src/foundation/` | 基础层 |
| runtime 组装规则和版本清单 | `depoly/packaging/runtime/` | 构建与交付配置，不是环境本体 |
| 产品内 Codex 的指令与技能资源 | `src/view/resources/agent/` | Agent 运行资源，不是开发 Agent 的仓库规则 |

根目录 `AGENTS.md` 面向维护仓库的开发 Agent；`src/view/resources/agent/` 面向软件内部 Codex。不得将两者混用。

源码、真实运行环境和用户项目数据分开管理。Python 解释器、PyTorch 等完整环境是交付资源，不作为源码提交；用户图片、训练记录及模型数据的实际存放布局另行设计。目前 `depoly/packaging/python/` 仅有目录占位，没有 Python 包配置、训练代码或运行环境。

### 8.3 bin 目录约定

- `bin/` 用于归集编译结果，按 `windows/`、`linux/` 分开，避免不同平台产物混放。
- 可存放生成的主可执行文件及配套运行文件，不放源码或用户项目数据。
- 当前 Cargo 的构建结果在 `depoly/target/`，本次已将 Linux 骨架可执行文件手动归集到 `bin/linux/vision-hyper-agent`；操作说明见 README。
- 生成的 `bin/` 内容已加入 `.gitignore`。空的 `bin/` 目录不通过 Git 保留，新工作副本在归集产物时按需建立。
- 尚未配置自动归集、安装器或 Windows 构建，不能把本机骨架产物当作完整软件发行包。

### 8.4 depends 目录约定

`src/depends/` 管理第三方依赖的来源、版本说明及受控材料，当前只包含 [README](../src/depends/README.md)，没有实际依赖或下载内容。

它不替代 Cargo/Python 的依赖声明，不承担运行时环境管理，也不与 `depoly/packaging/runtime/` 的组装规则混用。下载归档、缓存和预编译材料的生成目录已按约定忽略；具体依赖接入方式及版本仍待确认。

## 9. 下一步只逐层补充模块

后续再为每层明确实际模块及其职责、输入输出、依赖、状态和错误边界，并检查相关能力的界面入口、Agent 工具和 Skill 覆盖关系。

目录骨架和单 Cargo 包已经建立，但不预设各层具体业务模块的名称和数量，不将六层机械映射为六个独立包。后续按用户确认的模块范围继续设计和实现；具体接口协议、安装布局、功能依赖版本和验收参数仍待确认。

本期不引入前后端双主程序、内部 ZeroMQ 总线、微服务或通用插件系统。若以后确有需要，重新评估并由用户确认。

## 文档关系

- [产品需求文档](产品需求文档.md)：做什么、产品约束与未决需求。
- 本文：整体职责、调用与运行边界，以及代码和编译产物的目录规划。
- [团队编码规范](团队编码规范.md)：未来编码与协作的工程规则。
- [AGENTS.md](../AGENTS.md)：开发 Agent 按任务获取上下文的入口。

本文是软件框架说明，不是模块实现清单或实施计划。
