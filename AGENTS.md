# AGENTS.md

本文件是开发 Agent 修改本仓库时必须遵守的整体要求，目的是防止工作偏离项目方向。它不是产品内部 Agent 的提示词，也不记录具体接口、实现步骤或界面尺寸。

适用于本仓库及所有子目录。用户最新的明确要求优先；遇到旧文档与新要求不一致时，先说明，不按旧文档擅自改回去。

## 项目方向

- VisionHyperAgent 是 Agent 驱动的视觉模型桌面软件。首版只做实例分割：导入图片、交互标注、用户确认、自动训练与选模、离线识别。
- 使用 Rust + Slint，保持一个 Cargo 包、一个主程序。暂不拆前后端，不建立软件内部的 ZeroMQ 总线。
- 直接使用开源 Codex CLI 和 Ultralytics 官方 YOLO 技能，不另写同类 Agent 引擎，不增加独立辅助标注模型。
- 训练使用软件自行管理的 Python、PyTorch 和 YOLO26，在本机计算，不要求用户配置 Conda 或 Python。
- 生产识别使用 Rust、`ultralytics-inference` 和 ONNX Runtime，不依赖 Python 训练进程、互联网或在线 Agent。
- 支持 Linux、Windows 和 NVIDIA GPU。按软件版本的运行环境要求判断兼容性，不设置显卡型号白名单。相机使用 OpenCV，外部图片通信先使用 ZeroMQ。
- 当前已实现桌面界面和基础模块，Agent、训练、推理、相机等业务尚未接入。界面能显示不等于业务能运行，已有平台产物不等于已验证本轮修改。

## 架构要求

- **View 只负责界面交互。** 显示状态、接收操作、连接对应的 ViewModel，不直接调用参数管理器、驱动或 Agent，不在界面里实现业务。
- **ViewModel 按功能划分。** 每个 ViewModel 负责自己的交互和运行逻辑，可调用基础层、驱动层和 Agent，但不依赖 Slint 窗口、控件或事件循环。
- **主窗口不是总业务控制器。** `MainWindowViewModel` 负责主窗口导航、布局和子模块协调；配置操作由 `ConfigViewModel` 负责，其他模块同样各管自己的功能。
- **基础功能统一管理。** 日志、用户配置和软件内部参数放在 Foundation。用户配置与软件自动维护的内部参数分开，不因为界面需要使用就把管理对象放进 View。
- **同一功能只实现一次。** 按钮和聊天使用同一套执行逻辑，不能各写一套。底层通过结果或通知与调用方交互，不直接操作界面。
- **无 UI 也能使用核心逻辑。** ViewModel 应能脱离窗口运行和验证，但这不表示要新增无界面产品模式。主程序必须保留基础模块的正确初始化、状态保存和正常退出收尾。

## 项目目录说明

所有对象按职责放置，不能为了调用方便随意选目录。

| 目录 | 存放内容 |
| --- | --- |
| `src/view/` | 主窗口和连接 Slint、ViewModel 的代码，不放业务对象或参数文件读写实现。 |
| `src/view/pages/` | 完整的功能页面，如运行页、模型页、标注页、训练页、设置页和关于页。 |
| `src/view/components/` | 由多个控件组成的界面部分，如标题栏、导航项和 Agent 聊天面板。 |
| `src/view/ui_items/` | 页面、组合组件和资源目录之外的 UI 功能，如主题、提示状态、UI 数据类型和 Splitter。 |
| `src/view/resources/` | 软件资源；`ui/` 放图片、图标和字体，`agent/` 放产品 Agent 的指令和技能资源。 |
| `src/view_model/` | 各功能对应的 ViewModel。 |
| `src/agent/` | Codex 接入、技能管理、Agent 工作流程和工具调用。 |
| `src/drivers/` | 外部库、设备和执行进程的接口，如 YOLO／ONNX、相机、ZeroMQ 和 Python Worker。 |
| `src/foundation/` | 共用基础功能，包括 `Logger`、`ConfigManager` 和 `AppParam`。 |
| `src/depends/` | 第三方依赖的来源、版本和相关材料。 |
| `depoly/` | 打包、工具、测试和缓存；保留现有目录拼写。分别使用 `packaging/`、`tools/`、`tests/`、`target/` 子目录，Python 训练源码位于 `depoly/packaging/python/`。 |
| `bin/linux/`、`bin/windows/` | 各平台编译出的程序，不提交 Git。 |

- 类、结构体、单例、模块和数据类型都要放到对应位置。数据类型跟随管理它的模块；界面专用类型才放在 View。
- `main.rs` 负责程序入口，`lib.rs` 组织模块，`mod.rs` 负责导出和必要连接，不用来堆放新业务对象。基础层已有的共享错误和路径代码保留在 `mod.rs`。
- `src/application/` 已删除，不自行恢复。新增基础层文件、另建目录或迁移已有对象前，先取得用户同意。归属已经明确的对象直接按规则放置。
- 发现旧文件位置不合适时先说明，不借机批量搬动。测试、缓存、编译产物不能混进 View；完整运行环境、模型和用户数据不能当作源码存放。

## 代码规范

- 命名、格式、错误处理和并发要求遵守[团队编码规范](docs/团队编码规范.md)，本文件不重复列出语言细节。
- 保持职责清楚，优先使用简单、可读、可维护的实现。只做本次必要修改，不为未确定的需求提前搭框架。
- 现有公开接口和保存规则已经确认，未经同意不随意增删或改变，不恢复用户明确删除的接口。
- 错误必须明确返回或报告，不能吞掉错误、伪造成功，不能为了提速省略必要的错误处理和资源释放。
- 依赖版本和许可证不随任务顺手调整。核查第三方行为时使用项目所选版本的官方资料，区分官方说明与本项目实际验证结果。

## UI 整体要求

- 只做原生 Slint 界面，不制作网页原型。保持亮色、绚彩渐变、半透明磨砂和大圆角，不擅自换成灰白、保守的风格。
- 主题和重复样式统一管理，复用已有组件。一个 Slint 组件一个文件，文件名与组件名一致；具体命名要求见团队编码规范。
- 各功能面板与 Agent 面板使用统一的外框、标题栏和图标按钮。沿用已确认的组件样式和尺寸，不为单个页面另做一套；关于页保留必要的第三方署名。
- 独立功能面板之间使用 Splitter。布局要能调整，也要保证文字可读、面板不会被拖没。切页和缩放不能丢失用户草稿或布局偏好。
- Rust 主程序保存并恢复布局，内部布局参数与用户配置分开。独立 Viewer 仅用于预览，保留自动刷新，不另做布局保存功能。
- 保留已确认的导航和全局聊天交互，启动进入运行页，不增加首页。不常驻显示开发说明、预览标识或无关版本提示。
- 不把未接入的功能显示为执行成功，不编造 Agent 分析结果。预标注内容改变后，旧分析和确认结果不能继续当作有效结果。界面暂不显示“实例分割”字样，但产品范围不变。

## 工作流程与范围

默认流程：**确认目标 → 定位相关文件 → 完成最小修改 → 必要检查 → 简短汇报并停止。**

- 已明确的要求不反复询问。新增功能、改变接口或调整已确认方案前，先说明范围和方案，并取得必要确认。
- 修改前检查 Git 状态，保留用户已有改动。只读相关代码、调用方和必要资料；已经读过且没变化的内容不重复全文读取，不为了“保险”扫描整个仓库。
- 只做本次需要的格式检查、编译和直接相关的已有 Rust 测试。Slint 修改需要编译检查，并查看实际渲染；文档任务只做相关文档检查，不额外编译工程。
- 代码、环境和检查目标没变时，沿用本轮已通过的结果。失败后先看原因，不反复执行相同命令碰运气。
- 大范围重构、专项测试、全量检查、双平台构建、集中复制编译产物或发布打包，须先说明理由并取得同意。不能因为目录和工具已经存在，就自动追加工作。
- 达到本次目标和必要检查要求后立即结束。范围外问题简短指出，不顺手继续处理；遇到阻塞及时说明。
- 汇报实际修改、验证结果、未执行项和风险。编译通过不等于功能全部通过，未做过的测试、平台运行、审查或 CI 不能说已通过。

### 当前暂停的工作

以下规则优先于其他文档中的默认流程，直到用户明确恢复；用户单独要求某项工作时，只执行指定范围。

- **暂停测试脚本**：不新增、不运行临时验证程序或 Python／Shell 自动化测试脚本，不恢复已删除的专项测试。已有脚本保留；相关的已有 Rust 测试和 Slint 直接预览仍可按需执行。
- **暂停主动更新文档**：代码任务不附带修改 README、AGENTS.md、设计文档或生成测试报告。说明过时就简短指出；用户明确要求修改文档时除外。

## 安全与 Git 规则

- 不提交凭据、用户图片、数据集、模型权重、完整运行环境、日志或编译缓存。明文 KEY 也不能进入日志或诊断展示。
- 只按授权把任务图片发送给配置的在线服务，不发送无关文件或其他数据。训练计算留在本机，生产识别不依赖互联网。
- 使用项目内工具和已确定的依赖，不擅自修改用户全局环境、安装系统驱动或改变模型及推理设置。
- 用户要求后才提交、推送或合并；要求提交不等于允许推送。不自行重写 Git 历史，不恢复用户已删除的文件。

## 详细资料在哪里

只读本次任务相关的部分；文档和实现存在差异时，结合用户最新要求核实，不把旧说明当作已经完成的功能。

| 需要了解的内容 | 阅读位置 |
| --- | --- |
| 项目介绍、构建方法和平台说明 | [README](README.md)、`Cargo.toml`、`.cargo/config.toml` |
| 产品功能和未确定事项 | [产品需求文档](docs/产品需求文档.md) |
| 架构设计背景 | [软件框架](docs/软件框架.md)；当前目录以本文件和实际工程为准 |
| 具体编码要求 | [团队编码规范](docs/团队编码规范.md) |
| Slint 开发和项目内工具 | [Slint 技能](.agents/skills/slint/SKILL.md)、[工具说明](depoly/tools/slint/README.md) |
| 日志和用户配置的详细约定 | [Logger 设计](docs/plans/foundation/Logger.md)、[ConfigManager 设计](docs/plans/foundation/ConfigManager.md)及相关实现 |
| 软件内部参数与布局数据 | `src/foundation/app_param.rs` 及对应 ViewModel、View 的连接代码 |
