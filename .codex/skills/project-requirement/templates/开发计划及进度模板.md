# <项目名称> 开发计划及进度

> 按需输出到 `specs/plans/开发计划及进度.md`，只用于规划和跟踪业务代码开发。依据已确认的需求、技术方案、模块／组件 API 和代码规范制定任务，不在此记录环境安装、设计阶段确认或需求变更审批；也不复制 API 文档里的 2119 要求正文。现有计划应局部更新，不能用空模板覆盖已有任务。成品删除本段模板说明和不适用的占位行。

## 1. 开发目标与范围

| 项目 | 内容 |
| --- | --- |
| 本次开发目标 | <要交付的功能或模块及其可观察结果> |
| 开发范围 | <本计划覆盖的模块、组件或任务> |
| 依据的契约 | <实际使用的产品需求、技术方案、模块／组件 API 和代码规范路径；标出需要实现的 2119 要求 ID> |
| 不包含的工作 | <本次明确不做的开发工作；没有则写无> |
| 完成标准 | <代码、测试、评审和可验证产物应满足的实际条件> |

只列本次开发真正要用的文档和要求 ID。契约尚未明确时先确认相关 API，不把计划中的猜测当作已确认接口。

## 2. 开发任务计划

一个任务对应可检查的交付结果；按依赖顺序列出。任务 ID 使用本计划内稳定的 `DEV-01`、`DEV-02` 等标识，**不要与 2119 要求 ID 混用**。模块和组件分别规划，但组件始终归属于所属模块。

| 任务 ID | 所属模块／组件 | 开发任务及交付物 | 对应 API／2119 要求 ID | 前置任务 | 验收方式 |
| --- | --- | --- | --- | --- | --- |
| DEV-01 | <架构层／模块／组件> | <要实现或修改的代码及可检查的交付物> | <有效 API 文档路径、实际要求 ID> | <任务 ID；没有则写无> | <应运行的测试、检查及明确的通过条件> |

测试任务可单独列行；如测试随实现任务交付，则在该行写明测试入口与通过条件。不要凭计划创建源码、测试或编译产物，也不要把“计划编写测试”写成测试已通过。

## 3. 开发进度

计划确定后按实际执行情况更新同一任务 ID；状态使用“未开始、进行中、阻塞、待验收、已完成”，不要用文件存在或主观估计替代完成证据。

| 任务 ID | 状态 | 已完成的工作与证据 | 测试／2119 状态 | 阻塞与下一步 |
| --- | --- | --- | --- | --- |
| DEV-01 | <实际状态> | <已交付文件、提交或可核验结果；没有则写未开始> | <实际测试命令及结果、2119 要求覆盖与独立评审状态；尚未运行则注明> | <具体阻塞和下一行动；没有则写无> |

仅在相关实现、测试和必要的质量评审真实完成并通过时，将任务标为“已完成”。2119 的 `check: PASS` 若为零要求，不代表模块功能已测试；计划中的风险或阻塞未消除时如实记录，不伪造测试、评审或发布结果。
