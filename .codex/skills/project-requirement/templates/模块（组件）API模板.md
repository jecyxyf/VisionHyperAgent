# <模块名称／组件名称> API 文档

> 本模板同时适用于模块和组件；两者各自成文，只描述本对象的公开接口，不把组件写成父模块的接口摘要。模块文档保存到 `specs/models/<架构层名称>/<模块名>.md`，组件文档保存到 `specs/models/<架构层名称>/<模块名>/components/<组件名>.md`，并由所属模块引用。
>
> 第 1 章必填；第 2—5 章只写实际存在的接口，无对应内容则省略整章，保留其他章节编号。接口名称及范围以已确认的技术方案为准；成品须删除模板说明和占位内容。语言、签名和类型采用实际项目的表达方式，不套用特定技术栈。

## 1. 功能

说明当前模块或组件提供的能力、服务对象及可观察的结果。组件注明所属模块；存在上下游接口或依赖时简要说明调用方向并引用已有契约，不另写详细设计或实现流程。

| 项目 | 内容 |
| --- | --- |
| 对象 | <模块／组件名称；组件注明所属模块及其 API 文档> |
| 功能 | <提供的能力及使用场景> |
| 对外结果 | <调用方能得到的结果或通知> |
| 设计依据 | <已确认的技术方案中对应模块／组件及接口清单的位置> |

## 2. 数据结构（可选）

只定义公开接口实际使用且归属于本对象的结构体及其他公开数据类型；其他对象定义的类型引用其已有契约，不重复定义。

### 2.1 <结构体／类型名称>

**用途与定义**：<用途、实际类型声明或定义位置>。

| 字段／成员 | 类型／值 | 含义 | 约束或默认值（如有） |
| --- | --- | --- | --- |
| <字段／成员名称> | <类型；嵌套类型引用定义> | <表达的内容> | <必填、可空、范围或默认值等实际约束> |

枚举、类型别名等按实际类型改写字段表；没有字段的类型直接说明定义和取值即可。不为模板增加项目并未使用的数据类型。

## 3. 属性（可选）

按实际公开属性填写类型、作用、访问权限及必要的取值约束；不把内部字段当作公开属性。

| 属性 | 类型 | 功能／含义 | 访问方式 | 约束或默认值（如有） |
| --- | --- | --- | --- | --- |
| <属性名称> | <准确类型；复杂类型引用第 2 章或既有定义> | <属性表示的内容> | <只读／只写／读写及实际入口> | <可空、取值范围、初始值等实际约束> |

## 4. 方法（可选）

逐项描述实际公开的方法；也可记录技术方案中非消息触发的独立回调接口，但要注明实际调用方，不伪造成对象成员方法。每项以功能、输入和输出为主；只有关键方法需要补充简要实现原理。

### 4.1 <方法名称>

**功能**：<此接口做什么；必要时说明谁调用，以及可观察的结果>。

**签名**：

```text
<实际语言的完整方法／回调签名，包含参数和返回类型>
```

**输入参数**：

| 参数 | 类型 | 含义 | 必要约束（如有） |
| --- | --- | --- | --- |
| <参数名称> | <准确类型；复杂类型引用定义> | <输入内容及用途> | <必填、可空、默认值、范围等适用约束> |

无输入参数时写“无”。

**输出参数／返回值**：

| 输出 | 类型 | 含义与有效条件 |
| --- | --- | --- |
| <返回值或输出参数名称> | <准确类型；复杂类型引用定义> | <返回内容、成功或失败时是否有效> |

没有输出时写“无”。异步接口如先返回受理结果、稍后产生最终结果，应分别写清实际取得结果的接口；具体失败反馈、状态变化或资源约束仅在影响调用方时补充在本方法内，不另设固定表格。

**实现原理（仅关键方法按需填写）**：<用几句话解释主要思路及必要的实现约束；不罗列私有实现步骤>。

## 5. 消息回调（可选）

仅记录消息到达后被调用的公开接口，例如 C# 消息事件处理方法或 C++ 消息处理函数指针；它们可实现同一种消息处理能力，但按目标语言写真实签名。普通请求的同步响应不称为回调，应写在对应方法的输出中。

### 5.1 <消息回调名称>

**功能**：<处理什么消息、何时触发、谁提供及谁调用此接口>。

**签名**：

```text
<实际语言的完整回调签名或回调类型定义>
```

**输入参数**：

| 参数 | 类型 | 含义 | 必要约束（如有） |
| --- | --- | --- | --- |
| <参数名称> | <准确类型；消息载荷引用第 2 章或既有定义> | <消息内容或上下文> | <可空、有效期、取值范围等适用约束> |

无输入参数时写“无”。

**输出参数／返回值**：

| 输出 | 类型 | 含义与有效条件 |
| --- | --- | --- |
| <返回值或输出参数名称> | <准确类型> | <是否反馈处理结果、是否影响后续流程> |

没有输出时写“无”。如果调用方必须知道注册方式、触发时机、异常反馈或对象有效期，可在此接口下简要补充，不因模板强制发明注册／注销接口。
