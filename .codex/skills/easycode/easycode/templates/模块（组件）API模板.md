# REQ-NNN: <模块或组件名称> API

> 依据已确认的技术方案，为每个需要公开契约的模块或组件单独成文。模块保存到 `specs/models/<架构层>/REQ-NNN-<模块名>.md`；组件保存到 `specs/models/<架构层>/<模块名>/components/REQ-NNN-<组件名>.md`，并由所属模块文档引用。编号全项目唯一且保持稳定。`## Overview` 和 `## Requirements` 为本项目 2119 扫描所需的固定标题，后者必须放在末章；成品删除模板说明和占位内容，实际没有的接口类别整章省略。

## Overview

说明对象名称、架构归属、公开接口范围及技术方案依据。以下章节描述契约，末章列出开发和测试时应满足的可验证行为。

## 1. 功能与归属

| 项目 | 内容 |
| --- | --- |
| 对象与所属模块 | <模块名；若是组件，写所属模块与调用入口> |
| 职责与对外结果 | <能做什么，调用方能观察到什么> |
| 设计依据 | <技术方案中的对应模块或组件> |
| 依赖与调用方向 | <实际存在的上下游及所引用的契约> |

## 2. 公开数据类型（按需）

### <类型名称>

说明用途、字段与类型、有效范围和归属；其他模块已有定义的类型只引用，不重复声明。

## 3. 公开属性（按需）

| 属性 | 类型 | 用途 | 读写方式 | 约束 |
| --- | --- | --- | --- | --- |
| <属性名> | <实际类型> | <含义> | <实际访问方式> | <适用的限制> |

## 4. 方法与普通回调（按需）

### <接口名称>

- 功能与调用方：<作用、谁调用、可观察结果>。
- 签名：<目标语言或协议中的实际表达>。
- 输入：<参数、类型、含义和必要约束；没有则写无>。
- 输出：<返回值或输出参数的含义与有效条件；没有则写无>。
- 失败与状态：<调用方必须了解的失败反馈、状态变化或资源约束；没有则省略>。

## 5. 消息回调（按需）

### <回调名称>

说明触发条件、提供方与调用方、实际签名、输入和输出；同步请求的返回值应放回对应方法，不冒充消息回调。

## 6. 模块调用实例

只用前文已确认的接口说明一个实际调用场景和预期结果。模块文档展示上层调用方如何使用模块；组件文档展示所属模块如何使用组件。无法给出真实签名时先确认，不用假代码填充。

## Requirements

以下标题中的 `REQ-NNN` 与文件编号一致；列表中的条目由工具按章节和位置生成完整 ID，不在正文手写第二套编号。每条要求只写一个可观察且可验证的行为，使用一个大写 RFC 2119 关键词，如 `MUST`、`MUST NOT` 或 `SHOULD`；不发明接口、数值或失败场景。

### REQ-NNN.1: 开发要求

1. <实际接口及适用条件> MUST <调用方能观察并验证的行为>。

### REQ-NNN.2: 测试要求

1. <实际接口在真实边界或状态下> MUST <具体预期输出、反馈或状态>。

测试条目要说明场景及预期结果，不以“应编写测试”代替行为要求。成品按真实接口增删条目，保持已发布编号稳定。文档完成只说明要求已明确；真实测试、测试中的要求 ID、独立质量评审及 2119 完整门禁留给开发计划追踪。未执行时明确标注，不能声称已通过。
