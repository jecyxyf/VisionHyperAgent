# <项目名称> 代码规范

> 依据项目实际语言、已确认技术方案、接口文档、开发计划及有效项目规则编写，输出到 `specs/REQ-004-代码规范.md`。完成后回查开发计划中的格式化、构建与测试命令。成品移除本段说明和所有占位内容；不把其他项目的特殊要求当成默认规则。

## 1. 适用范围与依据

| 语言与文件范围 | 参考规范及来源 | 查阅状态 | 项目已有规则 |
| --- | --- | --- | --- |
| <实际语言、手写源码及例外> | <对应语言的可核实规范> | <已核实或待核实> | <优先适用的实际规则> |

先核对实际语言是否有适用的 Google 官方代码规范；若没有，使用该语言的官方规范，并遵守项目明确要求。没有可核实来源时如实标注，不杜撰规范名称、查阅日期或项目工具。

## 2. 命名、格式与依赖

按实际语言分别说明文件、类型、函数、变量与常量的命名，缩进与换行，以及导入、包含和模块依赖方向；明确与现有格式化配置的关系。

## 3. 接口、注释与错误处理

说明公开接口与内部实现的边界、输入校验、错误表达、日志和资源释放。按项目有效规则写注释，确保中文注释有实际功能信息；不为凑数量重复描述代码或破坏语法。

## 4. 并发与测试（适用时）

说明实际存在的线程或异步边界、状态归属、取消及资源管理规则；列出对应模块的单元、集成或人工验证方法。未采用并发的项目不强加并发机制。

## 5. 工具与检查

| 用途 | 实际命令或配置 | 状态 | 适用范围 |
| --- | --- | --- | --- |
| <格式化、静态分析、构建或测试> | <真实可用的命令；未配置时说明建议> | <已配置、建议或待核实> | <实际文件范围> |
| 2119 要求核对 | <项目本地 CLI 命令及实际扫描配置> | <已配置与已执行分别标注> | <正式要求及模块 API> |

区分规范来源、工具已配置、hooks 已触发和检查已执行；编译成功或零要求的 2119 检查通过不等于功能测试通过。测试要求的真实验证、标注和质量评审按开发计划执行。
